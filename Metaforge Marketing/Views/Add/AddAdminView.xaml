<UserControl x:Class="Metaforge_Marketing.Views.Add.AddAdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Metaforge_Marketing.Views.Add"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="720" d:DesignWidth="1280">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content="Add Admin" HorizontalAlignment="Center" VerticalAlignment="Center" 
               FontSize="24" FontWeight="Bold"/>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center">
            <DockPanel>
                <Label Content="Name"/>
                <TextBox MinWidth="100" Text="{Binding AdminToAdd.Name, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"/>
            </DockPanel>
            <DockPanel LastChildFill="False">
                <Button Content="Save" Command="{Binding SaveCommand}"/>
            </DockPanel>
        </StackPanel>
        
    </Grid>
</UserControl>
